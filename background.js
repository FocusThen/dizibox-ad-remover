console.log("Created By FocusThen. Github ==> https://github.com/FocusThen");

// let toggle = false;
// window.word = "off";
// chrome.runtime.onMessage.addListener(receiver);
// toggle = !toggle;
// function receiver(request, sender, sendResponse) {
//   if (request.text == "toggle") {
//     toggle = !toggle;
//     if (toggle) {
//       window.word = "on";
//       chrome.tabs.executeScript(tab.id, { file: "CSS.main.css" });
//     } else {
//       window.word = "off";
//       chrome.tabs.executeScript(null, { code: "alert()" });
//     }
//   }
// }
